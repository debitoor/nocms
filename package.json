{
	"name": "nocms",
	"version": "1.2.0",
	"description": "",
	"private": true,
	"scripts": {
		"bootstrap": "lerna bootstrap",
		"build": "lerna run build --parallel",
		"build-updated": "lerna run build --since --parallel",
		"build-watch": "lerna run build-watch --parallel",
		"clean": "lerna clean",
		"publish": "lerna publish",
		"test": "lerna run test --parallel && npm run test-e2e",
		"test-updated": "lerna run test --since --parallel",
		"updated": "lerna updated",
		"install-e2e": "cd e2e-tests && npm install",
		"test-e2e": "cd e2e-tests && npm run test"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/debitoor/nocms.git"
	},
	"author": "Debitoor",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/debitoor/nocms/issues"
	},
	"devDependencies": {
		"lerna": "3.4.3"
	}
}
